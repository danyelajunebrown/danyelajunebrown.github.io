# Copy this file to .env and fill in your values
# NEVER commit .env to git!

# Cloud API Configuration (for local worker)
CLOUD_API_URL=https://your-app-name.onrender.com
WORKER_API_KEY=get-this-from-render-dashboard
WORKER_ID=dell-laptop-01

# Local Worker Settings
WORK_DIR=/home/damages/worker_data
POLL_INTERVAL=30

# Optional: Storage Configuration
S3_BUCKET=your-bucket-name
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=us-east-1
